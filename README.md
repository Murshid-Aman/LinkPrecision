# LinkPrecision
Resolves Facebook share &amp; redirect links to their clean canonical URLs. Returns OG title, description, and post image — the same way WhatsApp previews do it. Built with Flask + BeautifulSoup.
